/// Enum representing different roles a participant can have in a chat room
enum ParticipantRole {
  /// Regular participant with basic permissions
  member,
  
  /// Participant with moderation capabilities
  moderator,
  
  /// Participant with full administrative permissions
  admin,
}